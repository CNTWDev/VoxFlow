{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "main-capability",
  "description": "Default capability for Vox Flow",
  "windows": ["main", "overlay"],
  "permissions": [
    "core:default",
    "shell:default",
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
