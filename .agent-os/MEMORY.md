# Memory

- Vox Flow is a Rust + Tauri voice input app.
- Keep Agent OS infrastructure independent from the voice input runtime unless explicitly integrating it.
- Prefer Markdown files for Agent OS storage until session or skill lookup needs exceed simple filesystem access.
